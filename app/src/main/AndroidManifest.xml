<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proxymall">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Splash"
            android:theme="@android:style/Theme.Black.NoTitleBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.MainActivity"/>
        <activity android:name=".activities.locationManagerStateDiscovery"/>
        <activity android:name=".activities.MallActivity"/>
        <activity android:name=".activities.CategoryProductActivity"/>
        <activity android:name=".activities.Login"/>
        <activity android:name=".activities.Cart"/>
        <activity android:name=".activities.Wishlist"/>
        <activity android:name=".activities.SearchActivity"/>
        <activity android:name=".activities.RegisterWithEmail"/>
        <activity android:name=".activities.goods_info"/>
        <activity android:name=".activities.mallInfo"
            android:theme="@android:style/Theme.Holo.Light.Dialog.NoActionBar"/>


    </application>

</manifest>